<%= form_for(jury_challenge_membership) do |f| %>
  <% if jury_challenge_membership.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(jury_challenge_membership.errors.count, "error") %> prohibited this jury_challenge_membership from being saved:</h2>

      <ul>
      <% jury_challenge_membership.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id_id %>
    <%= f.text_field :user_id_id %>
  </div>

  <div class="field">
    <%= f.label :challenge_id_id %>
    <%= f.text_field :challenge_id_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
