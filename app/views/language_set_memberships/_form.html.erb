<%= form_for(language_set_membership) do |f| %>
  <% if language_set_membership.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(language_set_membership.errors.count, "error") %> prohibited this language_set_membership from being saved:</h2>

      <ul>
      <% language_set_membership.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :language_id_id %>
    <%= f.text_field :language_id_id %>
  </div>

  <div class="field">
    <%= f.label :language_set_id_id %>
    <%= f.text_field :language_set_id_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
